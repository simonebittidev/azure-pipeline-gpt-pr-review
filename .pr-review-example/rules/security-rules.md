# Security Rules

Add project-specific security requirements here.
The LLM will apply them in addition to the standard security checks.

## Examples

- All user inputs must be validated and sanitized before use.
- Secrets must never be logged, even in debug mode.
- External HTTP calls must enforce a timeout.
