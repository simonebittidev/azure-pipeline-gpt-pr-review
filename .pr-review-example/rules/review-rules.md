# Review Rules

Add project-specific coding standards and quality rules here.
The LLM will apply them when reviewing each changed file.

## Examples

- All public methods must have XML/JSDoc documentation.
- Magic numbers must be replaced with named constants.
- Exception handling must never swallow errors silently.
